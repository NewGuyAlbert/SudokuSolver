public class Line {
    //number represents the identifier for either a row or a column
    public int number;
    //type represent if the Line object is either a row or column || true = row / false = column
    public boolean type;

    public Line(){}
}
